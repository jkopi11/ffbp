function parseESPN () {
	var headerRow = document.querySelectorAll('.playerTableBgRowSubhead td'),
    playerRows = document.querySelectorAll(".pncPlayerRow"),
    columns = [],
    markup = '',
    player, row, data;

	for (var i = 0; i < headerRow.length; i++) {
	    columns.push(headerRow[i].innerHTML);
	}
	debugger;
	for (var ii = 0; ii < playerRows.length; ii++) {
		player = playerRows[ii];
		row = player.querySelectorAll('td');
		debugger;
		markup += row[columns.indexOf('SLOT')].innerHTML + '<br>';
//		markup += row[columns.indexOf('PLAYER, TEAM POS') + '<br>'].querySelector('a').innerHTML + '<br>';
		markup += row[columns.indexOf('PROJ')] + '<br>';
	}
	return markup;
}

parseESPN();